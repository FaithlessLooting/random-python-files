PSUEDOCODE
translated = ""
plaintext - INPUT("what would you like to encode?")
plaintext = str.lower(plaintext)
f = OPENFILE jsontester.txt, WRITE
WRITE plaintext
CLOSE
OPEN jsontester.txt READ
data =""
lines = READ line by line
FOR line IN lines
	data= data + text in line
	PRINT(data)
CLOSE

STRING (data)

plaintext = LOWER.plaintext
keyword = INPUT what would you like the keyword to be
keyword = LOWER string
keyword = keyword*10
FUNCTION getTranslatedMessage(plaintext, keyword)
	translated = ""
	key = []
	FOR symbol IN keyword:
		IF symbol isalpha
		   num 2 = unicode(symbol)
		   num2 = num2-96
		   key APPEND (num2)	
	FOR i IN RANGE LENGTH plaintext
	IF plaintext isalpha plaintext
		num = ORD(symbol)
		   
	